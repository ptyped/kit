const express = require('express')
const router = express.Router()

/**
 * Add routes below here. E.g:
 */

/*
 * A basic GET route
 * 
 * router.get('/example-route', (req, res) => {
 *  res.render('examples/template, { param: "value" })
 * }
 */

 /**
  * Basic branching logic
  * 
  * router.post('/example-route', (req, res) => {
  *     const exampleField = req.session.data['exampleField]
  *     const shouldGoLeft = exampleField === "left"
  *     const shouldGoRight = exampleField === "right"
  * 
  *     if (shouldGoLeft) {
  *         res.redirect('/example-route/left/)
  *     } else if (shouldGoRight) {
  *         res.redirect('/example-route/left/)
  *     } else {
  *         res.redirect('/example-route)
  *     }
  * })
  */

module.exports = router